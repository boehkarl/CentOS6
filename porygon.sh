#Script to automate backups and recovery
#Ideas:
#Backup /etc /var /bin separate from /opt which contains splunk
#Make options for if I want to restore /etc /var /bin  OR  /opt

#One option for regular tar backup
#One option for compressed backup
